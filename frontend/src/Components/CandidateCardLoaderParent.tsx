import React from 'react';
import Grid2 from '@mui/material/Unstable_Grid2';
export default function CandidateCardLoaderParent({ children }: { children: React.ReactNode | React.ReactNode[] }) {
    return (
        <div>
            {children}
        </div>
        
    )
}